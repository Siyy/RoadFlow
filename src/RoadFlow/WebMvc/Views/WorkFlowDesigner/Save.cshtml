@{
    Layout = null;
    
    string json = Request.Form["json"];
    string msg = new Business.Platform.WorkFlow().SaveFlow(json);
    Business.Platform.Log.Add("保存了流程", json + "(" + msg + ")", Business.Platform.Log.Types.流程相关);
    Response.Write(msg);
}
